<%= image_tag "#{@pet.image}", height: '400', width: 'auto' %>
<h2>Name: <%= "#{@pet.name}" %></h2>
<p>
  Description: <%= "#{@pet.description}" %>
  <br>
  Approximate Age: <%= "#{@pet.age}" %>
  <br>
  Sex: <%= "#{@pet.sex}" %>
  <br>
  Adoption Status: <%= "#{@pet.status}" %>
</p>
